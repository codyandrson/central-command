# The one file that holds secrets, and everything generated per install.
.env
installed.manifest
setup-diagnostics.txt
setup-log.txt
uvicorn.log
uvicorn.pid
# The cockpit-driven updater's working dir (staged zip, runner copy, status)
.update/
