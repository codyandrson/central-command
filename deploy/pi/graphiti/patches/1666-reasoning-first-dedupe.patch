--- a/graphiti_core/prompts/dedupe_edges.py	2026-08-15 16:52:08.561816080 -0600
+++ b/graphiti_core/prompts/dedupe_edges.py	2026-08-15 16:52:08.609816456 -0600
@@ -22,6 +22,16 @@
 
 
 class EdgeDuplicate(BaseModel):
+    reasoning: str = Field(
+        ...,
+        description=(
+            'Step-by-step reasoning about duplicates and contradictions, written '
+            'BEFORE the final answer. For every duplicate or contradiction you '
+            'identify, explicitly state its idx number in this reasoning '
+            "(e.g. 'idx 2 is contradicted because...') — do not just describe "
+            "the fact's content, name the idx."
+        ),
+    )
     duplicate_facts: list[int] = Field(
         ...,
         description='List of idx values of duplicate facts (only from EXISTING FACTS range). Empty list if none.',
